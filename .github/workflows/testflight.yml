name: TestFlight Upload

# Manually-triggered TestFlight build + upload. Signing is done in CI with an
# App Store Connect API key (no certificates/profiles to export) via
# -allowProvisioningUpdates. Run it from the Actions tab → "TestFlight Upload" →
# Run workflow.
#
# Required repo secrets (Settings → Secrets and variables → Actions):
#   ASC_KEY_ID         App Store Connect API key ID (e.g. ABC123XYZ)
#   ASC_ISSUER_ID      API key issuer id (UUID)
#   ASC_KEY_P8_BASE64  the .p8 key file, base64-encoded
#   APPLE_TEAM_ID      your 10-char Apple Developer Team ID
#
# One-time prerequisites (see TESTFLIGHT.md):
#   - Active Apple Developer Program membership.
#   - An app record in App Store Connect with bundle id com.refx.app.

on:
  workflow_dispatch:
    inputs:
      build_number:
        description: "Build number (must be unique; defaults to the run number)"
        required: false
        default: ""
  # Tag trigger so it can be fired from the feature branch (workflow_dispatch
  # requires the workflow on the default branch). Push a tag like testflight-1.
  push:
    tags:
      - "testflight-*"

jobs:
  upload:
    runs-on: macos-15
    timeout-minutes: 45
    env:
      SCHEME: "ReFxApp (Release)"
      BUNDLE_ID: com.refx.app
    steps:
      - uses: actions/checkout@v4

      - name: Verify secrets are present
        run: |
          missing=0
          for v in ASC_KEY_ID ASC_ISSUER_ID ASC_KEY_P8_BASE64 APPLE_TEAM_ID; do
            if [ -z "${!v}" ]; then echo "::error::Missing secret $v"; missing=1; fi
          done
          [ "$missing" = "0" ] || exit 1
        env:
          ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
          ASC_KEY_P8_BASE64: ${{ secrets.ASC_KEY_P8_BASE64 }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Write App Store Connect API key
        env:
          ASC_KEY_P8_BASE64: ${{ secrets.ASC_KEY_P8_BASE64 }}
        run: |
          mkdir -p "$RUNNER_TEMP/keys"
          KEY="$RUNNER_TEMP/keys/AuthKey.p8"
          # The secret may hold either the raw .p8 (PEM) or its base64 encoding.
          # Detect which, and normalise to a valid PEM file. Tolerant of stray
          # whitespace / line wrapping introduced when the secret was pasted.
          if printf '%s' "$ASC_KEY_P8_BASE64" | grep -q 'BEGIN PRIVATE KEY'; then
            printf '%s\n' "$ASC_KEY_P8_BASE64" > "$KEY"
          else
            printf '%s' "$ASC_KEY_P8_BASE64" | tr -d '[:space:]' | base64 --decode > "$KEY" 2>/dev/null || true
          fi
          if ! grep -q 'BEGIN PRIVATE KEY' "$KEY"; then
            echo "::error::ASC_KEY_P8_BASE64 did not yield a valid .p8 key. Paste either the raw AuthKey_*.p8 contents (with the BEGIN/END lines) or its base64."
            exit 1
          fi
          echo "API key written OK ($(wc -c < "$KEY" | tr -d ' ') bytes)"

      - name: Resolve build number
        id: bn
        run: |
          BN="${{ github.event.inputs.build_number }}"
          [ -n "$BN" ] || BN="${{ github.run_number }}"
          echo "value=$BN" >> "$GITHUB_OUTPUT"
          echo "Using build number $BN"

      - name: Archive
        run: |
          xcodebuild archive \
            -project ReFxApp.xcodeproj \
            -scheme "$SCHEME" \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath "$RUNNER_TEMP/ReFxApp.xcarchive" \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$RUNNER_TEMP/keys/AuthKey.p8" \
            -authenticationKeyID "${{ secrets.ASC_KEY_ID }}" \
            -authenticationKeyIssuerID "${{ secrets.ASC_ISSUER_ID }}" \
            DEVELOPMENT_TEAM="${{ secrets.APPLE_TEAM_ID }}" \
            CODE_SIGN_STYLE=Automatic \
            CURRENT_PROJECT_VERSION="${{ steps.bn.outputs.value }}"

      - name: Export & upload to TestFlight
        run: |
          cat > "$RUNNER_TEMP/ExportOptions.plist" <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key><string>app-store</string>
            <key>destination</key><string>upload</string>
            <key>signingStyle</key><string>automatic</string>
            <key>teamID</key><string>${{ secrets.APPLE_TEAM_ID }}</string>
          </dict>
          </plist>
          EOF
          xcodebuild -exportArchive \
            -archivePath "$RUNNER_TEMP/ReFxApp.xcarchive" \
            -exportOptionsPlist "$RUNNER_TEMP/ExportOptions.plist" \
            -exportPath "$RUNNER_TEMP/export" \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$RUNNER_TEMP/keys/AuthKey.p8" \
            -authenticationKeyID "${{ secrets.ASC_KEY_ID }}" \
            -authenticationKeyIssuerID "${{ secrets.ASC_ISSUER_ID }}"

      - name: Clean up key
        if: always()
        run: rm -f "$RUNNER_TEMP/keys/AuthKey.p8"
