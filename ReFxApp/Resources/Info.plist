<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key>
    <string>ReFx</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>

    <!-- The app uses only standard HTTPS/TLS (exempt encryption), so TestFlight
         won't prompt for export-compliance docs on each upload. -->
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>

    <!-- Live Activities (server op progress on the lock screen / Dynamic Island). -->
    <key>NSSupportsLiveActivities</key>
    <true/>

    <!-- Deep-link scheme used by the Home Screen widget (refxapp://servers). -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key>
            <string>com.refx.app</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>refxapp</string>
            </array>
        </dict>
    </array>

    <!-- Default API/Web origins, injected from the active .xcconfig. Overridable
         at runtime in Settings (persisted to UserDefaults; never holds tokens). -->
    <key>ReFxAPIScheme</key>
    <string>$(API_SCHEME)</string>
    <key>ReFxAPIHost</key>
    <string>$(API_HOST)</string>
    <key>ReFxWebScheme</key>
    <string>$(WEB_SCHEME)</string>
    <key>ReFxWebHost</key>
    <string>$(WEB_HOST)</string>

    <key>UILaunchScreen</key>
    <dict>
        <key>UIColorName</key>
        <string>LaunchBackground</string>
    </dict>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- Face ID rationale (app-lock). -->
    <key>NSFaceIDUsageDescription</key>
    <string>Unlock ReFx to view your servers, console and files.</string>

    <!-- Background App Refresh: wake periodically to check server health and
         notify on outages. iOS controls the actual cadence. -->
    <key>BGTaskSchedulerPermittedIdentifiers</key>
    <array>
        <string>com.refx.app.refresh</string>
    </array>
    <key>UIBackgroundModes</key>
    <array>
        <string>fetch</string>
    </array>

    <!-- App Transport Security: HTTPS enforced. Debug builds relax cleartext to
         localhost only so a local panel-api over http works in the simulator. -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key>
        <false/>
        <key>NSExceptionDomains</key>
        <dict>
            <key>localhost</key>
            <dict>
                <key>NSExceptionAllowsInsecureHTTPLoads</key>
                <true/>
            </dict>
        </dict>
    </dict>
</dict>
</plist>
